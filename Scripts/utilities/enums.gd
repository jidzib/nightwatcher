extends Node

enum ObjectCategory {
	DEFAULT,
	BREAKABLE,
	CROP
}

enum ObjectType {
	TREE,
	ROCK,
	CROP
}

enum BreakableType {
	TREE,
	ROCK
}

enum CropType {
	SUNFLOWER
}

enum ItemType {
	DEFAULT,
	PLANTABLE,
	HOE,
	WATER_BUCKET,
	AXE,
	PICKAXE
}

enum TileLayer {
	GRASS,
	FARM,
	WATER
}
