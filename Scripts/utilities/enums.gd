extends Node

enum ObjectCategory {
	DEFAULT,
	BREAKABLE,
	CROP
}

enum ObjectType {
	TREE,
	ROCK,
	CROP
}

enum BreakableType {
	TREE,
	ROCK
}

enum ItemType {
	DEFAULT,
	PLANTABLE,
	HOE,
	WATER_BUCKET,
	AXE,
	PICKAXE
}

enum ItemIDs {
	WOOD,
	STONE,
	HOE,
	SUNFLOWER,
	WATER_BUCKET,
	AXE,
	PICKAXE,
	DEV_TREE,
	DEV_ROCK
}

enum TileLayer {
	GRASS,
	WATER,
	FARM
}
