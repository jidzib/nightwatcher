extends Node

enum ObjectCategory {
	DEFAULT,
	BREAKABLE
}

enum ObjectType {
	FOREST_TREE,
	ROCK,
	FOLIAGE,
	SUNFLOWER,
	FARMLAND,
	JUNGLE_TREE,
	CACTUS,
	SNOW_TREE,
	BAMBOO_TREE
}

enum ItemType {
	DEFAULT,
	PLANTABLE,
	HOE,
	WATER_BUCKET,
	AXE,
	PICKAXE
}

enum ItemIDs {
	WOOD,
	STONE,
	HOE,
	SUNFLOWER,
	WATER_BUCKET,
	AXE,
	PICKAXE,
	DEV_TREE,
	DEV_ROCK,
	SUNFLOWER_SEEDS,
	ENTITY_SPAWNER
}

enum TileLayer {
	GRASS,
	WATER,
	FARM
}

enum EntityType {
	PLAYER,
	RED_PANDA,
	BUNNY,
	SNOW_LEOPARD
}

enum Tier {
	COMMON,
	UNCOMMON,
	RARE,
	LEGENDARY,
	OTHERWORLDY
}

enum NeedType {
	HUNGER,
	ENERGY
}

enum TerrainType {
	BLOCKED,
	GROUND,
	WATER
}

enum UIType {
	MAIN_MENU,
	WORLD_SELECT_MENU,
	CREATE_WORLD_MENU
}
